{
  "name": "farmstead-rental",
  "version": "1.0.0",
  "description": "A project to contribute with my parents to management of farmstead rental",
  "author": "Patrick",
  "private": true,
  "license": "MIT",
  "scripts": {
    "docker": "docker-compose down && docker-compose up --build",
    "db": "docker-compose down && docker-compose up db --build",
    "server": "docker-compose down && docker-compose up db server --build"
  }
}
